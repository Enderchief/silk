/// an internal type module, only to be used by silk or when extending silk.
/// Always import from the module itself, not here.
pub type WxObject(t)

pub type WxEvtHandler(t)

pub type WxWindow(win)

pub type WxControl(t)

pub type WxWebView

pub type WxFrame

pub type WxSizerItem

pub type WxSizer(sizer)

pub type WxBoxSizer(a)

pub type WxStaticText

pub type WxStaticBoxSizer

pub type OkT {
  Ok
}
